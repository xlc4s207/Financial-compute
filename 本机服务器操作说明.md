# Fund OS 本机服务器操作说明

本机运行三个容器：

- `fund-os-web-1`：网页和反向代理。
- `fund-os-api-1`：保存、读取共享账目。
- `fund-os-db-1`：PostgreSQL 数据库。账目保存在 Docker 命名卷中。

外部访问只通过 Tailscale，不对公网开放。

## 每次开机后

1. 打开 **Docker Desktop**，等待左下角或状态栏显示 Engine running。
2. 打开 **Windows PowerShell**。
3. 依次输入：

```powershell
cd 'E:\日常任务\东江\Fund-OS'
docker compose up -d
docker compose ps
```

最后一条命令应显示三个容器都为 `Up`，其中数据库还应显示 `(healthy)`。

4. 确认 Tailscale 在运行。任务栏右下角应有 Tailscale 图标；没有时，从开始菜单打开 **Tailscale** 并登录。
5. 在 PowerShell 输入：

```powershell
tailscale serve status
```

应看到网页被代理到 `http://127.0.0.1:8180`。如果没有该行，重新输入：

```powershell
tailscale serve --bg --https=443 http://127.0.0.1:8180
```

## 打开网站

本机浏览器可打开：`http://127.0.0.1:8180/`

已加入同一 Tailscale 网络的设备打开：`https://xulch6.tailc179c5.ts.net/`

所有设备都必须先登录同一个 Tailscale tailnet。此地址不是公开网站；未加入 tailnet 的人无法访问。

## 让组员访问

1. 请组员在电脑或手机安装 Tailscale。
2. 在 Tailscale 管理后台的 **Users / 用户** 页面邀请其 Tailscale 账号加入你的 tailnet；或让管理员将其加入。
3. 组员接受邀请、在 Tailscale 客户端登录后，再打开上面的 HTTPS 地址。
4. 先由一位组员新增一笔测试流水，另一台设备在约 15 秒内刷新或等待同步，确认两端都能看见同一笔数据；随后删除测试流水。

## 修改网页代码后重新部署

网页容器只复制前端文件，不会直接读取整个项目目录。每次修改 `index.html`、`styles.css`、`app.js`、`reimbursements.js` 或 `server-sync.js` 后，在项目目录运行：

```powershell
docker compose build web
docker compose up -d --no-deps web
```

如果改了 `api` 目录中的后端文件，则运行：

```powershell
docker compose up -d --build api web
```

Docker Hub 网络偶尔断开时，先确认 Docker Desktop 的代理仍为 `http://127.0.0.1:7897`，然后再次执行同一条命令。

## 日常备份与恢复

- 在网页右上角点 **导出**，保存 JSON 文件。这是最简单的人工备份方式。
- JSON 文件应保存在项目目录之外或同步到受信任的网盘；项目目录中的 `fund-os-backup-*.json` 已被 Git 忽略，不会提交到 GitHub。
- 需要恢复时，在网页右上角点 **导入**，选择导出的 JSON。恢复会覆盖当前共享账目，所以操作前先再次导出一份当前数据。

## 查看状态、停止与重启

```powershell
cd 'E:\日常任务\东江\Fund-OS'
docker compose ps
docker compose logs --tail=100 api
docker compose stop
docker compose up -d
```

`docker compose stop` 只停止容器，不会删除数据库。不要执行 `docker compose down -v`；其中的 `-v` 会删除数据库卷和所有账目。